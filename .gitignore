node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Secrets. `.env*.local` alone does not match a bare `.env`, and the
# scroll-site skill keeps a live Higgsfield key in one beside its SKILL.md.
.env
.env.*
!.env.example
.claude/commands/**/.env
